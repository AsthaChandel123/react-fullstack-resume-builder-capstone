{
  "name": "student-portfolio-builder",
  "private": true,
  "scripts": {
    "start": "npm --prefix resume-builder start",
    "test": "npm --prefix resume-builder test -- --watchAll=false",
    "build": "npm --prefix resume-builder build"
  }
}